<<echo=FALSE, results=hide>>=
questions <- c(
  "possuem a mesma inércia.",
  "estão sujeitas à mesma força resultante.",
  "têm a mesma quantidade de movimento inicial.",
  "adquirem a mesma aceleração durante a queda.",
  "são lançadas com a mesma velocidade horizontal."
)
solutions <- c(FALSE, FALSE, FALSE, TRUE, FALSE)

ordem <- sample(seq_along(questions))
questions <- questions[ordem]
solutions <- solutions[ordem]
@

\usepackage[utf8]{inputenc}

\begin{question}

(ENEM 2016) Para um salto no Grand Canyon usando motos, dois paraquedistas vão utilizar uma moto cada, sendo que uma delas possui massa três vezes maior. Foram construídas duas pistas idênticas até a beira do precipício, de forma que no momento do salto as motos deixem a pista horizontalmente e ao mesmo tempo. No instante em que saltam, os paraquedistas abandonam suas motos e elas caem praticamente sem resistência do ar. As motos atingem o solo simultaneamente porque

<<echo=FALSE, results=hide, results=tex>>=
answerlist(questions)
@

\end{question}

\begin{solution}

Após deixarem as pistas, as duas motos realizam movimento de projétil. Para determinar o instante em que atingem o solo, interessa apenas o movimento vertical.

As duas motos:
\begin{itemize}
  \item partem da mesma altura;
  \item têm velocidade vertical inicial nula, pois deixam a pista horizontalmente;
  \item estão sujeitas à mesma aceleração vertical, $g$, desprezada a resistência do ar.
\end{itemize}

Assim, a equação vertical é a mesma para ambas:
\[
\Delta y=\frac{1}{2}gt^2.
\]

A massa da moto não aparece nessa relação. Portanto, as duas atingem o solo ao mesmo tempo porque \textbf{adquirem a mesma aceleração durante a queda}.

\end{solution}

%% META-INFORMATION
%% Fonte: ENEM 2016.
%% Questao realocada de qtdmov_impulso para cinematica: sua resolucao depende da independencia do movimento vertical em relacao a massa e velocidade horizontal, nao de quantidade de movimento.
%% \extype{schoice}
%% \exsolution{\Sexpr{mchoice2string(solutions, single = TRUE)}}
%% \exname{Q02testQmov}
%% \exsection{Cinematica; Lancamento horizontal; Queda livre; ENEM}
%% \exusepackage[utf8]{inputenc}
