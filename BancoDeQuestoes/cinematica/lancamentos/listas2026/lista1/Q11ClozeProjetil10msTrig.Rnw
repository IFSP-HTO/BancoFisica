<<echo=FALSE, results=hide>>=
g<-10; v0<-sample(c(10,20,30,40),1); sen<-0.8; cos<-0.6
vx<-v0*cos; vy<-v0*sen; H<-vy^2/(2*g); T<-2*vy/g; A<-vx*T
fmt<-function(x,d=2) formatC(x,format="f",digits=d,decimal.mark=",")
@
\usepackage[utf8]{inputenc}

\begin{question}
Um projétil é lançado do solo com velocidade $\Sexpr{fmt(v0,0)}\,\mathrm{m/s}$, formando um ângulo $\alpha$ tal que $\sin\alpha=0{,}8$ e $\cos\alpha=0{,}6$. Adote $g=10\,\mathrm{m/s^2}$. Determine:
\begin{answerlist}
  \item altura máxima, em $\mathrm{m}$;
  \item alcance horizontal, em $\mathrm{m}$.
\end{answerlist}
\end{question}
\begin{solution}
$H=v_{0y}^2/(2g)=\Sexpr{fmt(H)}$ e $A=v_{0x}(2v_{0y}/g)=\Sexpr{fmt(A)}$.
\begin{answerlist}
  \item $\Sexpr{fmt(H)}$
  \item $\Sexpr{fmt(A)}$
\end{answerlist}
\end{solution}

%% META-INFORMATION
%% Fonte: Professor Panosso, exercício 3, parametrizado.
%% \extype{cloze}
%% \exsolution{\Sexpr{H}|\Sexpr{A}}
%% \exclozetype{num|num}
%% \extol{0.05|0.1}
%% \exname{LO26L1Q11}
%% \exsection{Mecanica; Cinematica; Lancamento obliquo; Listas 2026}
